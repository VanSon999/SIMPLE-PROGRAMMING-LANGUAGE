var art, bit, cer:integer;
                var d:boolean;
                var ert:real;
                var m: array [1 .. 100] of integer;
                function foo():integer; 
                begin  
                    return 0; 
                end
                procedure main(); 
                begin 
                    bit := foo();
                    d := true;
                    art := m[0] + 1;
                    cer := m[d] + 1;
                    return ;
                end